# pi-delegate (archived)

`delegate` has moved to the global [pi-extensions](https://github.com/theMladyPan/pi-extensions) repository.

Clone that repository to `~/.pi/agent/extensions`, then restart Pi or run `/reload`.

This repository is retained only as a migration notice and will be archived.
